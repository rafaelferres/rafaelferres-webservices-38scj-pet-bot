## Serviço que insere dados na api do pets

Para rodar é só rodar o comando npm install && npm start

## Consumir api

Enviar post para /addBpm com o payload { bpm : number } para fazer o envio de um bpm
